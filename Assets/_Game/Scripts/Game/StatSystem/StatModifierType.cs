using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// default order values allow more flexibility in custom ordering later
public enum StatModifierType
{
    Flat = 100,
    PercentAdd = 200,
    PercentMultiply = 300
}
